# mycoder 환경 변수 설정 예제

# OpenAI API 엔드포인트 (LM Studio 또는 OpenAI 호환 서버)
MYCODER_OPENAI_BASE_URL=http://210.126.109.57:3620/v1

# API 키 (필요한 경우)
# MYCODER_OPENAI_API_KEY=your-api-key

# 채팅 모델 (기본값: qwen2.5-7b-instruct-1m)
MYCODER_CHAT_MODEL=qwen2.5-7b-instruct-1m

# 임베딩 모델 (기본값: text-embedding-nomic-embed-text-v1.5)
MYCODER_EMBEDDING_MODEL=text-embedding-nomic-embed-text-v1.5

# 서버 URL (기본값: http://localhost:8089)
# MYCODER_SERVER_URL=http://localhost:8089

# 임베딩 비활성화 (필요한 경우)
# MYCODER_DISABLE_EMBEDDINGS=1

# 임베딩 캐시 비활성화 (필요한 경우)
# MYCODER_EMBED_CACHE_DISABLE=1

# LLM 최소 간격 (밀리초)
# MYCODER_LLM_MIN_INTERVAL_MS=100